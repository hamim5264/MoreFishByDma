import 'package:get/get.dart';

/// Kept for compatibility with existing bindings.
/// Splash navigation is handled inside SplashScreenView (StatefulWidget),
/// so controller remains lightweight.
class SplashScreenController extends GetxController {}
